# webserver with the gui

the important information needed for the gui is in ./localhost/ , the folders are symlinked with 
```
ln -s <path to the file/folder to be linked> <the path of the link to be created>
```